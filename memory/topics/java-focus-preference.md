

## Java 主题聚焦约定

- 用户明确要求：**之后的所有对话均针对 Java 相关技术**，包括但不限于以下方向：
  - Spring Boot（含 Spring Boot 3 架构与分层规范）
  - MyBatis / MyBatis-Plus（动态查询、批量处理、性能陷阱）
  - JVM（参数配置、GC 日志解析、线程 dump 分析）
  - 多线程与并发工具类（`CompletableFuture`, `StampedLock`, `ThreadLocal` 等）
  - RAG 系统中 Java 侧的 Milvus 向量操作与文档分块逻辑
  - CPU 高占用的线程级根因分析（线程泄漏、Full GC、服务耦合异常等）
- 此偏好自 executionId `exec_ecc4c4e705be4d5aa827b1e52a33ee9e` 起生效，作为后续所有 Java 相关交互的默认上下文约束。